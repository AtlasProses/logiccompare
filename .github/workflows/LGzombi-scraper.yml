name: 🧟 Elit 2000+ Avcı Botları (Zero-Junk Trend Scrapers)

on:
  schedule:
    # Her 3 saatte bir otomatik arka planda uyanır
    - cron: '0 */3 * * *'
  workflow_dispatch:
    inputs:
      target:
        description: 'Hedeflenen Toplam Veri Sayısı (Varsayılan: 1000)'
        required: true
        default: '1000'
        type: string
      category:
        description: 'Kategori Seçimi (all, tech, finance, gaming, sports)'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - tech
          - finance
          - gaming
          - sports

# Çakışma Önleyici & Sıralı Bekleme Kuyruğu Kalkanı
concurrency:
  group: logiccompare-pipeline
  cancel-in-progress: false

jobs:
  hunt-and-enrich-pool:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
    
    steps:
      - name: Repoyu Klonla
        uses: actions/checkout@v4

      - name: Node.js Kurulumu
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Bağımlılıkları Yükle
        run: npm install

      - name: Elit Avcı Botları Çalıştır (50 Dakika Zaman Aşımı Korumalı)
        run: |
          TARGET="${{ inputs.target || '1000' }}"
          CATEGORY="${{ inputs.category || 'all' }}"
          node scripts/run_all_hunters.mjs --target=$TARGET --cat=$CATEGORY

      - name: Çekilen Altın Verileri GitHub'a Kaydet ve Push Et
        run: |
          git config --global user.name "ZombiAvciBot"
          git config --global user.email "bot@logiccompare.com"
          git add -A
          git commit -m "feat(pool): Elit Avcı Botlar havuzu 2000+ trend veriyle güncelledi" || echo "Yeni veri eklenmedi"
          git pull --rebase --autostash origin main || true
          git push origin main
